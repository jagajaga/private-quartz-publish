# Absolute path to your vault directory on this server. The stager mounts
# this read-only.
VAULT_DIR=/path/to/your/vault

# Public domain Caddy will serve and obtain a Let's Encrypt cert for. Must
# resolve to this server's public IP via DNS.
PUBLISH_DOMAIN=notes.example.com
