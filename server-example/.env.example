# Path to the directory where your Obsidian vault lives on the server.
# The stager mounts this read-only.
VAULT_DIR=/path/to/your/vault

# The hostname your reverse proxy will serve the published site on.
# Used in the example Caddyfile and Quartz config.
PUBLISH_DOMAIN=notes.example.com
